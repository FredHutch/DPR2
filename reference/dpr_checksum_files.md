# dpr_checksum_files

This function will return an md5 checksum of an in-memory R object.

## Usage

``` r
dpr_checksum_files(paths)
```

## Arguments

- paths:

  path of RDA files to hash

## Value

a character string of the md5 hashes of a files loaded into memory.
