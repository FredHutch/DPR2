# dpr_data_checksums

Return table of current in-memory checksums for RDA files in the data
directory.

## Usage

``` r
dpr_data_checksums(path = ".")
```

## Arguments

- path:

  path to data package

## Value

a data.frame containing the name of the rda object and the corresponding
checksum

## Author

jmtaylor
